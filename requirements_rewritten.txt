# 공통: 세 파일 전반에서 직접 import 되는 패키지
torch
transformers>=4.45.0
opencv-python
pillow
numpy

# 최신 main.py 전용 또는 사실상 필수
paddleocr

# old DINO 계열(main_old_DINO(2nd).py, main_old_object.py)에서 필요
torchvision
pycocotools
matplotlib
scipy
huggingface_hub

# GroundingDINO import용
# 주의: 환경에 따라 pip 패키지보다 로컬 소스를 editable install 하는 편이 안정적일 수 있음
groundingdino

# 선택 사항 / 환경에 따라 필요할 수 있는 보조 패키지
accelerate
sentencepiece
einops

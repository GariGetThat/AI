# requirements.txt

# 영상 처리
opencv-python>=4.8.0

# 수치 연산
numpy==1.24.4
scipy==1.10.1

# 얼굴 분석: detection + alignment + recognition embedding
# InsightFace >=0.2부터 onnxruntime backend 사용
insightface==0.7.3

# GPU 서버 사용 시
onnxruntime-gpu==1.16.3

# CPU만 사용할 경우에는 위 줄 대신 아래 사용
# onnxruntime==1.16.3

# 추적
# ByteTrack 직접 연결 시 설치가 안 될 수도 있어서 우선 주석 유지 가능
# bytetracker>=0.2.0

# 클러스터링 / embedding 기반 track 병합용
scikit-learn>=1.3.0

# 기타
tqdm>=4.66.0